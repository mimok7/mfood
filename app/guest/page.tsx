export const dynamic = 'force-dynamic'

export default async function GuestPage() {
  return (
    <section>
      <h2 style={{fontSize:'18px', fontWeight:600}}>게스트</h2>
      <p style={{marginTop:8}}>비로그인 사용자나 손님 대상 화면입니다.</p>
    </section>
  )
}
